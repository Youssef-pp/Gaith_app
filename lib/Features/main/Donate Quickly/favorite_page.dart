import 'package:flutter/material.dart';
class DonateQuicklyPage extends StatelessWidget {
  const DonateQuicklyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text('donate _quickly')),
    );
  }
}
